#ifndef __SAMD21_SYSCTRL_H_
#define __SAMD21_SYSCTRL_H_

/* clock_init_defaults() sets the CPU core clock
 * to the DFLL48 clock running at 48MHz in closed-loop mode */
void clock_init_defaults(void);

#endif
